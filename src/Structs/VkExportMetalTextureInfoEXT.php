<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkImageAspectFlagBits;
use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkExportMetalTextureInfoEXT\VkExportMetalTextureInfoEXT as ExtVkExportMetalTextureInfoEXT;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkExportMetalTextureInfoEXT">, 7 members.
 *
 * The typed shape of the array ExtVkExportMetalTextureInfoEXT::pack() takes and
 * ExtVkExportMetalTextureInfoEXT::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkExportMetalTextureInfoEXT
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::EXPORT_METAL_TEXTURE_INFO_EXT,
        public int $pNext = 0,
        public int $image = 0,
        public int $imageView = 0,
        public int $bufferView = 0,
        public VkImageAspectFlagBits|int $plane = 0,
        public int $mtlTexture = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['image'] = $this->image;
        $members['imageView'] = $this->imageView;
        $members['bufferView'] = $this->bufferView;
        $members['plane'] = $this->plane instanceof \BackedEnum ? $this->plane->value : $this->plane;
        $members['mtlTexture'] = $this->mtlTexture;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            image: (int) ($members['image'] ?? 0),
            imageView: (int) ($members['imageView'] ?? 0),
            bufferView: (int) ($members['bufferView'] ?? 0),
            plane: (int) ($members['plane'] ?? 0),
            mtlTexture: (int) ($members['mtlTexture'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkExportMetalTextureInfoEXT::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkExportMetalTextureInfoEXT::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkExportMetalTextureInfoEXT::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkExportMetalTextureInfoEXT::size();
    }
}
