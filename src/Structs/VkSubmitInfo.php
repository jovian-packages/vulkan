<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkSubmitInfo\VkSubmitInfo as ExtVkSubmitInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkSubmitInfo">, 9 members.
 *
 * The typed shape of the array ExtVkSubmitInfo::pack() takes and
 * ExtVkSubmitInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkSubmitInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::SUBMIT_INFO,
        public int $pNext = 0,
        public int $waitSemaphoreCount = 0,
        public int $pWaitSemaphores = 0,
        public int $pWaitDstStageMask = 0,
        public int $commandBufferCount = 0,
        public int $pCommandBuffers = 0,
        public int $signalSemaphoreCount = 0,
        public int $pSignalSemaphores = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['waitSemaphoreCount'] = $this->waitSemaphoreCount;
        $members['pWaitSemaphores'] = $this->pWaitSemaphores;
        $members['pWaitDstStageMask'] = $this->pWaitDstStageMask;
        $members['commandBufferCount'] = $this->commandBufferCount;
        $members['pCommandBuffers'] = $this->pCommandBuffers;
        $members['signalSemaphoreCount'] = $this->signalSemaphoreCount;
        $members['pSignalSemaphores'] = $this->pSignalSemaphores;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            waitSemaphoreCount: (int) ($members['waitSemaphoreCount'] ?? 0),
            pWaitSemaphores: (int) ($members['pWaitSemaphores'] ?? 0),
            pWaitDstStageMask: (int) ($members['pWaitDstStageMask'] ?? 0),
            commandBufferCount: (int) ($members['commandBufferCount'] ?? 0),
            pCommandBuffers: (int) ($members['pCommandBuffers'] ?? 0),
            signalSemaphoreCount: (int) ($members['signalSemaphoreCount'] ?? 0),
            pSignalSemaphores: (int) ($members['pSignalSemaphores'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkSubmitInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkSubmitInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkSubmitInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkSubmitInfo::size();
    }
}
