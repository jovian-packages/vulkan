<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkFormat;
use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkRenderingAreaInfo\VkRenderingAreaInfo as ExtVkRenderingAreaInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkRenderingAreaInfo">, 7 members.
 *
 * The typed shape of the array ExtVkRenderingAreaInfo::pack() takes and
 * ExtVkRenderingAreaInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkRenderingAreaInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::RENDERING_AREA_INFO,
        public int $pNext = 0,
        public int $viewMask = 0,
        public int $colorAttachmentCount = 0,
        public int $pColorAttachmentFormats = 0,
        public VkFormat|int $depthAttachmentFormat = 0,
        public VkFormat|int $stencilAttachmentFormat = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['viewMask'] = $this->viewMask;
        $members['colorAttachmentCount'] = $this->colorAttachmentCount;
        $members['pColorAttachmentFormats'] = $this->pColorAttachmentFormats;
        $members['depthAttachmentFormat'] = $this->depthAttachmentFormat instanceof \BackedEnum ? $this->depthAttachmentFormat->value : $this->depthAttachmentFormat;
        $members['stencilAttachmentFormat'] = $this->stencilAttachmentFormat instanceof \BackedEnum ? $this->stencilAttachmentFormat->value : $this->stencilAttachmentFormat;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            viewMask: (int) ($members['viewMask'] ?? 0),
            colorAttachmentCount: (int) ($members['colorAttachmentCount'] ?? 0),
            pColorAttachmentFormats: (int) ($members['pColorAttachmentFormats'] ?? 0),
            depthAttachmentFormat: (int) ($members['depthAttachmentFormat'] ?? 0),
            stencilAttachmentFormat: (int) ($members['stencilAttachmentFormat'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkRenderingAreaInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkRenderingAreaInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkRenderingAreaInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkRenderingAreaInfo::size();
    }
}
