<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkDescriptorPoolCreateInfo\VkDescriptorPoolCreateInfo as ExtVkDescriptorPoolCreateInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkDescriptorPoolCreateInfo">, 6 members.
 *
 * The typed shape of the array ExtVkDescriptorPoolCreateInfo::pack() takes and
 * ExtVkDescriptorPoolCreateInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkDescriptorPoolCreateInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::DESCRIPTOR_POOL_CREATE_INFO,
        public int $pNext = 0,
        public int $flags = 0,
        public int $maxSets = 0,
        public int $poolSizeCount = 0,
        public int $pPoolSizes = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['flags'] = $this->flags;
        $members['maxSets'] = $this->maxSets;
        $members['poolSizeCount'] = $this->poolSizeCount;
        $members['pPoolSizes'] = $this->pPoolSizes;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            flags: (int) ($members['flags'] ?? 0),
            maxSets: (int) ($members['maxSets'] ?? 0),
            poolSizeCount: (int) ($members['poolSizeCount'] ?? 0),
            pPoolSizes: (int) ($members['pPoolSizes'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkDescriptorPoolCreateInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkDescriptorPoolCreateInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkDescriptorPoolCreateInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkDescriptorPoolCreateInfo::size();
    }
}
