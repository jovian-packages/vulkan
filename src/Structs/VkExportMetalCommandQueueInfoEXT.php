<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkExportMetalCommandQueueInfoEXT\VkExportMetalCommandQueueInfoEXT as ExtVkExportMetalCommandQueueInfoEXT;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkExportMetalCommandQueueInfoEXT">, 4 members.
 *
 * The typed shape of the array ExtVkExportMetalCommandQueueInfoEXT::pack() takes and
 * ExtVkExportMetalCommandQueueInfoEXT::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkExportMetalCommandQueueInfoEXT
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::EXPORT_METAL_COMMAND_QUEUE_INFO_EXT,
        public int $pNext = 0,
        public int $queue = 0,
        public int $mtlCommandQueue = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['queue'] = $this->queue;
        $members['mtlCommandQueue'] = $this->mtlCommandQueue;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            queue: (int) ($members['queue'] ?? 0),
            mtlCommandQueue: (int) ($members['mtlCommandQueue'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkExportMetalCommandQueueInfoEXT::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkExportMetalCommandQueueInfoEXT::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkExportMetalCommandQueueInfoEXT::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkExportMetalCommandQueueInfoEXT::size();
    }
}
