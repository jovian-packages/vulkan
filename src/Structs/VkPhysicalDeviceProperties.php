<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkPhysicalDeviceType;
use Vulkan\Struct\VkPhysicalDeviceProperties\VkPhysicalDeviceProperties as ExtVkPhysicalDeviceProperties;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkPhysicalDeviceProperties">, 9 members.
 *
 * The typed shape of the array ExtVkPhysicalDeviceProperties::pack() takes and
 * ExtVkPhysicalDeviceProperties::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkPhysicalDeviceProperties
{
    public function __construct(
        public int $apiVersion = 0,
        public int $driverVersion = 0,
        public int $vendorID = 0,
        public int $deviceID = 0,
        public VkPhysicalDeviceType|int $deviceType = 0,
        public string $deviceName = '',
        public array $pipelineCacheUUID = [],
        public ?VkPhysicalDeviceLimits $limits = null,
        public ?VkPhysicalDeviceSparseProperties $sparseProperties = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['apiVersion'] = $this->apiVersion;
        $members['driverVersion'] = $this->driverVersion;
        $members['vendorID'] = $this->vendorID;
        $members['deviceID'] = $this->deviceID;
        $members['deviceType'] = $this->deviceType instanceof \BackedEnum ? $this->deviceType->value : $this->deviceType;
        $members['deviceName'] = $this->deviceName;
        $members['pipelineCacheUUID'] = $this->pipelineCacheUUID;
        if (! is_null($this->limits)) {
            $members['limits'] = $this->limits->toArray();
        }
        if (! is_null($this->sparseProperties)) {
            $members['sparseProperties'] = $this->sparseProperties->toArray();
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            apiVersion: (int) ($members['apiVersion'] ?? 0),
            driverVersion: (int) ($members['driverVersion'] ?? 0),
            vendorID: (int) ($members['vendorID'] ?? 0),
            deviceID: (int) ($members['deviceID'] ?? 0),
            deviceType: (int) ($members['deviceType'] ?? 0),
            deviceName: (string) ($members['deviceName'] ?? ''),
            pipelineCacheUUID: (array) ($members['pipelineCacheUUID'] ?? []),
            limits: is_array($members['limits'] ?? null) ? VkPhysicalDeviceLimits::fromArray($members['limits']) : null,
            sparseProperties: is_array($members['sparseProperties'] ?? null) ? VkPhysicalDeviceSparseProperties::fromArray($members['sparseProperties']) : null,
        );
    }

    public function pack(): int
    {
        return ExtVkPhysicalDeviceProperties::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkPhysicalDeviceProperties::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkPhysicalDeviceProperties::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkPhysicalDeviceProperties::size();
    }
}
