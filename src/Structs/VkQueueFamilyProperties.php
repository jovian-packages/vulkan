<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Vulkan\Struct\VkQueueFamilyProperties\VkQueueFamilyProperties as ExtVkQueueFamilyProperties;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkQueueFamilyProperties">, 4 members.
 *
 * The typed shape of the array ExtVkQueueFamilyProperties::pack() takes and
 * ExtVkQueueFamilyProperties::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkQueueFamilyProperties
{
    public function __construct(
        public int $queueFlags = 0,
        public int $queueCount = 0,
        public int $timestampValidBits = 0,
        public ?VkExtent3D $minImageTransferGranularity = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['queueFlags'] = $this->queueFlags;
        $members['queueCount'] = $this->queueCount;
        $members['timestampValidBits'] = $this->timestampValidBits;
        if (! is_null($this->minImageTransferGranularity)) {
            $members['minImageTransferGranularity'] = $this->minImageTransferGranularity->toArray();
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            queueFlags: (int) ($members['queueFlags'] ?? 0),
            queueCount: (int) ($members['queueCount'] ?? 0),
            timestampValidBits: (int) ($members['timestampValidBits'] ?? 0),
            minImageTransferGranularity: is_array($members['minImageTransferGranularity'] ?? null) ? VkExtent3D::fromArray($members['minImageTransferGranularity']) : null,
        );
    }

    public function pack(): int
    {
        return ExtVkQueueFamilyProperties::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkQueueFamilyProperties::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkQueueFamilyProperties::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkQueueFamilyProperties::size();
    }
}
