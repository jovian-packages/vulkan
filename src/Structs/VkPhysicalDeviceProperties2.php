<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkPhysicalDeviceProperties2\VkPhysicalDeviceProperties2 as ExtVkPhysicalDeviceProperties2;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkPhysicalDeviceProperties2">, 3 members.
 *
 * The typed shape of the array ExtVkPhysicalDeviceProperties2::pack() takes and
 * ExtVkPhysicalDeviceProperties2::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkPhysicalDeviceProperties2
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::PHYSICAL_DEVICE_PROPERTIES_2,
        public int $pNext = 0,
        public ?VkPhysicalDeviceProperties $properties = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        if (! is_null($this->properties)) {
            $members['properties'] = $this->properties->toArray();
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            properties: is_array($members['properties'] ?? null) ? VkPhysicalDeviceProperties::fromArray($members['properties']) : null,
        );
    }

    public function pack(): int
    {
        return ExtVkPhysicalDeviceProperties2::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkPhysicalDeviceProperties2::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkPhysicalDeviceProperties2::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkPhysicalDeviceProperties2::size();
    }
}
