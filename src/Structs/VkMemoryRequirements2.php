<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkMemoryRequirements2\VkMemoryRequirements2 as ExtVkMemoryRequirements2;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkMemoryRequirements2">, 3 members.
 *
 * The typed shape of the array ExtVkMemoryRequirements2::pack() takes and
 * ExtVkMemoryRequirements2::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkMemoryRequirements2
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::MEMORY_REQUIREMENTS_2,
        public int $pNext = 0,
        public ?VkMemoryRequirements $memoryRequirements = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        if (! is_null($this->memoryRequirements)) {
            $members['memoryRequirements'] = $this->memoryRequirements->toArray();
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            memoryRequirements: is_array($members['memoryRequirements'] ?? null) ? VkMemoryRequirements::fromArray($members['memoryRequirements']) : null,
        );
    }

    public function pack(): int
    {
        return ExtVkMemoryRequirements2::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkMemoryRequirements2::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkMemoryRequirements2::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkMemoryRequirements2::size();
    }
}
