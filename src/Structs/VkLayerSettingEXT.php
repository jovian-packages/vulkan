<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkLayerSettingTypeEXT;
use Vulkan\Struct\VkLayerSettingEXT\VkLayerSettingEXT as ExtVkLayerSettingEXT;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkLayerSettingEXT">, 5 members.
 *
 * The typed shape of the array ExtVkLayerSettingEXT::pack() takes and
 * ExtVkLayerSettingEXT::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkLayerSettingEXT
{
    public function __construct(
        public int $pLayerName = 0,
        public int $pSettingName = 0,
        public VkLayerSettingTypeEXT|int $type = 0,
        public int $valueCount = 0,
        public int $pValues = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['pLayerName'] = $this->pLayerName;
        $members['pSettingName'] = $this->pSettingName;
        $members['type'] = $this->type instanceof \BackedEnum ? $this->type->value : $this->type;
        $members['valueCount'] = $this->valueCount;
        $members['pValues'] = $this->pValues;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            pLayerName: (int) ($members['pLayerName'] ?? 0),
            pSettingName: (int) ($members['pSettingName'] ?? 0),
            type: (int) ($members['type'] ?? 0),
            valueCount: (int) ($members['valueCount'] ?? 0),
            pValues: (int) ($members['pValues'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkLayerSettingEXT::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkLayerSettingEXT::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkLayerSettingEXT::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkLayerSettingEXT::size();
    }
}
