<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkPhysicalDeviceMaintenance3Properties\VkPhysicalDeviceMaintenance3Properties as ExtVkPhysicalDeviceMaintenance3Properties;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkPhysicalDeviceMaintenance3Properties">, 4 members.
 *
 * The typed shape of the array ExtVkPhysicalDeviceMaintenance3Properties::pack() takes and
 * ExtVkPhysicalDeviceMaintenance3Properties::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkPhysicalDeviceMaintenance3Properties
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::PHYSICAL_DEVICE_MAINTENANCE_3_PROPERTIES,
        public int $pNext = 0,
        public int $maxPerSetDescriptors = 0,
        public int $maxMemoryAllocationSize = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['maxPerSetDescriptors'] = $this->maxPerSetDescriptors;
        $members['maxMemoryAllocationSize'] = $this->maxMemoryAllocationSize;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            maxPerSetDescriptors: (int) ($members['maxPerSetDescriptors'] ?? 0),
            maxMemoryAllocationSize: (int) ($members['maxMemoryAllocationSize'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkPhysicalDeviceMaintenance3Properties::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkPhysicalDeviceMaintenance3Properties::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkPhysicalDeviceMaintenance3Properties::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkPhysicalDeviceMaintenance3Properties::size();
    }
}
