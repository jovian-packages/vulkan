<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Vulkan\Struct\VkDescriptorBufferInfo\VkDescriptorBufferInfo as ExtVkDescriptorBufferInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkDescriptorBufferInfo">, 3 members.
 *
 * The typed shape of the array ExtVkDescriptorBufferInfo::pack() takes and
 * ExtVkDescriptorBufferInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkDescriptorBufferInfo
{
    public function __construct(
        public int $buffer = 0,
        public int $offset = 0,
        public int $range = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['buffer'] = $this->buffer;
        $members['offset'] = $this->offset;
        $members['range'] = $this->range;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            buffer: (int) ($members['buffer'] ?? 0),
            offset: (int) ($members['offset'] ?? 0),
            range: (int) ($members['range'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkDescriptorBufferInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkDescriptorBufferInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkDescriptorBufferInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkDescriptorBufferInfo::size();
    }
}
