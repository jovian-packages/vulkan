<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Vulkan\Struct\VkSparseBufferMemoryBindInfo\VkSparseBufferMemoryBindInfo as ExtVkSparseBufferMemoryBindInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkSparseBufferMemoryBindInfo">, 3 members.
 *
 * The typed shape of the array ExtVkSparseBufferMemoryBindInfo::pack() takes and
 * ExtVkSparseBufferMemoryBindInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkSparseBufferMemoryBindInfo
{
    public function __construct(
        public int $buffer = 0,
        public int $bindCount = 0,
        public int $pBinds = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['buffer'] = $this->buffer;
        $members['bindCount'] = $this->bindCount;
        $members['pBinds'] = $this->pBinds;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            buffer: (int) ($members['buffer'] ?? 0),
            bindCount: (int) ($members['bindCount'] ?? 0),
            pBinds: (int) ($members['pBinds'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkSparseBufferMemoryBindInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkSparseBufferMemoryBindInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkSparseBufferMemoryBindInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkSparseBufferMemoryBindInfo::size();
    }
}
