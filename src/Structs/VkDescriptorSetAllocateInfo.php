<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkDescriptorSetAllocateInfo\VkDescriptorSetAllocateInfo as ExtVkDescriptorSetAllocateInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkDescriptorSetAllocateInfo">, 5 members.
 *
 * The typed shape of the array ExtVkDescriptorSetAllocateInfo::pack() takes and
 * ExtVkDescriptorSetAllocateInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkDescriptorSetAllocateInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::DESCRIPTOR_SET_ALLOCATE_INFO,
        public int $pNext = 0,
        public int $descriptorPool = 0,
        public int $descriptorSetCount = 0,
        public int $pSetLayouts = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['descriptorPool'] = $this->descriptorPool;
        $members['descriptorSetCount'] = $this->descriptorSetCount;
        $members['pSetLayouts'] = $this->pSetLayouts;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            descriptorPool: (int) ($members['descriptorPool'] ?? 0),
            descriptorSetCount: (int) ($members['descriptorSetCount'] ?? 0),
            pSetLayouts: (int) ($members['pSetLayouts'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkDescriptorSetAllocateInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkDescriptorSetAllocateInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkDescriptorSetAllocateInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkDescriptorSetAllocateInfo::size();
    }
}
