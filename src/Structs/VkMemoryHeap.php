<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Vulkan\Struct\VkMemoryHeap\VkMemoryHeap as ExtVkMemoryHeap;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkMemoryHeap">, 2 members.
 *
 * The typed shape of the array ExtVkMemoryHeap::pack() takes and
 * ExtVkMemoryHeap::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkMemoryHeap
{
    public function __construct(
        public int $size = 0,
        public int $flags = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['size'] = $this->size;
        $members['flags'] = $this->flags;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            size: (int) ($members['size'] ?? 0),
            flags: (int) ($members['flags'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkMemoryHeap::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkMemoryHeap::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkMemoryHeap::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkMemoryHeap::size();
    }
}
