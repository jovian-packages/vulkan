<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkDebugUtilsMessengerCallbackDataEXT\VkDebugUtilsMessengerCallbackDataEXT as ExtVkDebugUtilsMessengerCallbackDataEXT;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkDebugUtilsMessengerCallbackDataEXT">, 12 members.
 *
 * The typed shape of the array ExtVkDebugUtilsMessengerCallbackDataEXT::pack() takes and
 * ExtVkDebugUtilsMessengerCallbackDataEXT::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkDebugUtilsMessengerCallbackDataEXT
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::DEBUG_UTILS_MESSENGER_CALLBACK_DATA_EXT,
        public int $pNext = 0,
        public int $flags = 0,
        public int $pMessageIdName = 0,
        public int $messageIdNumber = 0,
        public int $pMessage = 0,
        public int $queueLabelCount = 0,
        public int $pQueueLabels = 0,
        public int $cmdBufLabelCount = 0,
        public int $pCmdBufLabels = 0,
        public int $objectCount = 0,
        public int $pObjects = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['flags'] = $this->flags;
        $members['pMessageIdName'] = $this->pMessageIdName;
        $members['messageIdNumber'] = $this->messageIdNumber;
        $members['pMessage'] = $this->pMessage;
        $members['queueLabelCount'] = $this->queueLabelCount;
        $members['pQueueLabels'] = $this->pQueueLabels;
        $members['cmdBufLabelCount'] = $this->cmdBufLabelCount;
        $members['pCmdBufLabels'] = $this->pCmdBufLabels;
        $members['objectCount'] = $this->objectCount;
        $members['pObjects'] = $this->pObjects;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            flags: (int) ($members['flags'] ?? 0),
            pMessageIdName: (int) ($members['pMessageIdName'] ?? 0),
            messageIdNumber: (int) ($members['messageIdNumber'] ?? 0),
            pMessage: (int) ($members['pMessage'] ?? 0),
            queueLabelCount: (int) ($members['queueLabelCount'] ?? 0),
            pQueueLabels: (int) ($members['pQueueLabels'] ?? 0),
            cmdBufLabelCount: (int) ($members['cmdBufLabelCount'] ?? 0),
            pCmdBufLabels: (int) ($members['pCmdBufLabels'] ?? 0),
            objectCount: (int) ($members['objectCount'] ?? 0),
            pObjects: (int) ($members['pObjects'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkDebugUtilsMessengerCallbackDataEXT::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkDebugUtilsMessengerCallbackDataEXT::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkDebugUtilsMessengerCallbackDataEXT::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkDebugUtilsMessengerCallbackDataEXT::size();
    }
}
