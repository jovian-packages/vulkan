<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkColorSpaceKHR;
use Jovian\Bindings\Vulkan\Enums\VkFormat;
use Vulkan\Struct\VkSurfaceFormatKHR\VkSurfaceFormatKHR as ExtVkSurfaceFormatKHR;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkSurfaceFormatKHR">, 2 members.
 *
 * The typed shape of the array ExtVkSurfaceFormatKHR::pack() takes and
 * ExtVkSurfaceFormatKHR::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkSurfaceFormatKHR
{
    public function __construct(
        public VkFormat|int $format = 0,
        public VkColorSpaceKHR|int $colorSpace = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['format'] = $this->format instanceof \BackedEnum ? $this->format->value : $this->format;
        $members['colorSpace'] = $this->colorSpace instanceof \BackedEnum ? $this->colorSpace->value : $this->colorSpace;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            format: (int) ($members['format'] ?? 0),
            colorSpace: (int) ($members['colorSpace'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkSurfaceFormatKHR::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkSurfaceFormatKHR::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkSurfaceFormatKHR::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkSurfaceFormatKHR::size();
    }
}
