<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Vulkan\Struct\VkClearColorValue\VkClearColorValue as ExtVkClearColorValue;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="union" name="VkClearColorValue">, 3 members.
 *
 * The typed shape of the array ExtVkClearColorValue::pack() takes and
 * ExtVkClearColorValue::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 *
 * A union. Every member is nullable and defaults to null, because only
 * the one you wrote has a meaning; toArray() emits the members that are
 * not null, in registry order, and the last one written wins. unpack()
 * hands back every member, because which one is live is not knowable
 * from the bytes.
 */
final readonly class VkClearColorValue
{
    public function __construct(
        public ?array $float32 = null,
        public ?array $int32 = null,
        public ?array $uint32 = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        if (! is_null($this->float32)) {
            $members['float32'] = $this->float32;
        }
        if (! is_null($this->int32)) {
            $members['int32'] = $this->int32;
        }
        if (! is_null($this->uint32)) {
            $members['uint32'] = $this->uint32;
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            float32: isset($members['float32']) ? (array) $members['float32'] : null,
            int32: isset($members['int32']) ? (array) $members['int32'] : null,
            uint32: isset($members['uint32']) ? (array) $members['uint32'] : null,
        );
    }

    public function pack(): int
    {
        return ExtVkClearColorValue::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkClearColorValue::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkClearColorValue::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkClearColorValue::size();
    }
}
