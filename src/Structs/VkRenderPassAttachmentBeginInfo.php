<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkRenderPassAttachmentBeginInfo\VkRenderPassAttachmentBeginInfo as ExtVkRenderPassAttachmentBeginInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkRenderPassAttachmentBeginInfo">, 4 members.
 *
 * The typed shape of the array ExtVkRenderPassAttachmentBeginInfo::pack() takes and
 * ExtVkRenderPassAttachmentBeginInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkRenderPassAttachmentBeginInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::RENDER_PASS_ATTACHMENT_BEGIN_INFO,
        public int $pNext = 0,
        public int $attachmentCount = 0,
        public int $pAttachments = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['attachmentCount'] = $this->attachmentCount;
        $members['pAttachments'] = $this->pAttachments;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            attachmentCount: (int) ($members['attachmentCount'] ?? 0),
            pAttachments: (int) ($members['pAttachments'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkRenderPassAttachmentBeginInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkRenderPassAttachmentBeginInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkRenderPassAttachmentBeginInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkRenderPassAttachmentBeginInfo::size();
    }
}
