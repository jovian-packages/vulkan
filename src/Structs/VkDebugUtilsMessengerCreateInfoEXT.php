<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkDebugUtilsMessengerCreateInfoEXT\VkDebugUtilsMessengerCreateInfoEXT as ExtVkDebugUtilsMessengerCreateInfoEXT;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkDebugUtilsMessengerCreateInfoEXT">, 7 members.
 *
 * The typed shape of the array ExtVkDebugUtilsMessengerCreateInfoEXT::pack() takes and
 * ExtVkDebugUtilsMessengerCreateInfoEXT::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkDebugUtilsMessengerCreateInfoEXT
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::DEBUG_UTILS_MESSENGER_CREATE_INFO_EXT,
        public int $pNext = 0,
        public int $flags = 0,
        public int $messageSeverity = 0,
        public int $messageType = 0,
        public int $pfnUserCallback = 0,
        public int $pUserData = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['flags'] = $this->flags;
        $members['messageSeverity'] = $this->messageSeverity;
        $members['messageType'] = $this->messageType;
        $members['pfnUserCallback'] = $this->pfnUserCallback;
        $members['pUserData'] = $this->pUserData;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            flags: (int) ($members['flags'] ?? 0),
            messageSeverity: (int) ($members['messageSeverity'] ?? 0),
            messageType: (int) ($members['messageType'] ?? 0),
            pfnUserCallback: (int) ($members['pfnUserCallback'] ?? 0),
            pUserData: (int) ($members['pUserData'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkDebugUtilsMessengerCreateInfoEXT::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkDebugUtilsMessengerCreateInfoEXT::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkDebugUtilsMessengerCreateInfoEXT::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkDebugUtilsMessengerCreateInfoEXT::size();
    }
}
