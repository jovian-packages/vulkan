<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkPhysicalDeviceMemoryProperties2\VkPhysicalDeviceMemoryProperties2 as ExtVkPhysicalDeviceMemoryProperties2;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkPhysicalDeviceMemoryProperties2">, 3 members.
 *
 * The typed shape of the array ExtVkPhysicalDeviceMemoryProperties2::pack() takes and
 * ExtVkPhysicalDeviceMemoryProperties2::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkPhysicalDeviceMemoryProperties2
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::PHYSICAL_DEVICE_MEMORY_PROPERTIES_2,
        public int $pNext = 0,
        public ?VkPhysicalDeviceMemoryProperties $memoryProperties = null,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        if (! is_null($this->memoryProperties)) {
            $members['memoryProperties'] = $this->memoryProperties->toArray();
        }

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            memoryProperties: is_array($members['memoryProperties'] ?? null) ? VkPhysicalDeviceMemoryProperties::fromArray($members['memoryProperties']) : null,
        );
    }

    public function pack(): int
    {
        return ExtVkPhysicalDeviceMemoryProperties2::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkPhysicalDeviceMemoryProperties2::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkPhysicalDeviceMemoryProperties2::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkPhysicalDeviceMemoryProperties2::size();
    }
}
