<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkRenderPassBeginInfo\VkRenderPassBeginInfo as ExtVkRenderPassBeginInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkRenderPassBeginInfo">, 7 members.
 *
 * The typed shape of the array ExtVkRenderPassBeginInfo::pack() takes and
 * ExtVkRenderPassBeginInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkRenderPassBeginInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::RENDER_PASS_BEGIN_INFO,
        public int $pNext = 0,
        public int $renderPass = 0,
        public int $framebuffer = 0,
        public ?VkRect2D $renderArea = null,
        public int $clearValueCount = 0,
        public int $pClearValues = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['renderPass'] = $this->renderPass;
        $members['framebuffer'] = $this->framebuffer;
        if (! is_null($this->renderArea)) {
            $members['renderArea'] = $this->renderArea->toArray();
        }
        $members['clearValueCount'] = $this->clearValueCount;
        $members['pClearValues'] = $this->pClearValues;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            renderPass: (int) ($members['renderPass'] ?? 0),
            framebuffer: (int) ($members['framebuffer'] ?? 0),
            renderArea: is_array($members['renderArea'] ?? null) ? VkRect2D::fromArray($members['renderArea']) : null,
            clearValueCount: (int) ($members['clearValueCount'] ?? 0),
            pClearValues: (int) ($members['pClearValues'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkRenderPassBeginInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkRenderPassBeginInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkRenderPassBeginInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkRenderPassBeginInfo::size();
    }
}
