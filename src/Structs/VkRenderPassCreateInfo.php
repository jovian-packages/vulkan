<?php

declare(strict_types=1);

namespace Jovian\Bindings\Vulkan\Structs;

use Jovian\Bindings\Vulkan\Enums\VkStructureType;
use Vulkan\Struct\VkRenderPassCreateInfo\VkRenderPassCreateInfo as ExtVkRenderPassCreateInfo;

/**
 * GENERATED by scripts/generate.php. Do not hand-edit.
 *
 * Source: vk.xml <type category="struct" name="VkRenderPassCreateInfo">, 9 members.
 *
 * The typed shape of the array ExtVkRenderPassCreateInfo::pack() takes and
 * ExtVkRenderPassCreateInfo::unpack() returns. toArray() is this object flattened,
 * fromArray() is its inverse, and each of the four binding methods is
 * exactly one extension call.
 */
final readonly class VkRenderPassCreateInfo
{
    public function __construct(
        public VkStructureType|int $sType = VkStructureType::RENDER_PASS_CREATE_INFO,
        public int $pNext = 0,
        public int $flags = 0,
        public int $attachmentCount = 0,
        public int $pAttachments = 0,
        public int $subpassCount = 0,
        public int $pSubpasses = 0,
        public int $dependencyCount = 0,
        public int $pDependencies = 0,
    ) {}

    /**
     * @return array<string, mixed> the shape ext-vulkan's pack() takes
     */
    public function toArray(): array
    {
        $members = [];
        $members['sType'] = $this->sType instanceof \BackedEnum ? $this->sType->value : $this->sType;
        $members['pNext'] = $this->pNext;
        $members['flags'] = $this->flags;
        $members['attachmentCount'] = $this->attachmentCount;
        $members['pAttachments'] = $this->pAttachments;
        $members['subpassCount'] = $this->subpassCount;
        $members['pSubpasses'] = $this->pSubpasses;
        $members['dependencyCount'] = $this->dependencyCount;
        $members['pDependencies'] = $this->pDependencies;

        return $members;
    }

    /**
     * @param  array<string, mixed>  $members  the shape ext-vulkan's unpack() returns
     */
    public static function fromArray(array $members): self
    {
        return new self(
            sType: (int) ($members['sType'] ?? 0),
            pNext: (int) ($members['pNext'] ?? 0),
            flags: (int) ($members['flags'] ?? 0),
            attachmentCount: (int) ($members['attachmentCount'] ?? 0),
            pAttachments: (int) ($members['pAttachments'] ?? 0),
            subpassCount: (int) ($members['subpassCount'] ?? 0),
            pSubpasses: (int) ($members['pSubpasses'] ?? 0),
            dependencyCount: (int) ($members['dependencyCount'] ?? 0),
            pDependencies: (int) ($members['pDependencies'] ?? 0),
        );
    }

    public function pack(): int
    {
        return ExtVkRenderPassCreateInfo::pack($this->toArray());
    }

    public function packInto(int $ptr): void
    {
        ExtVkRenderPassCreateInfo::packInto($ptr, $this->toArray());
    }

    public static function unpack(int $ptr): self
    {
        return self::fromArray(ExtVkRenderPassCreateInfo::unpack($ptr));
    }

    public static function size(): int
    {
        return ExtVkRenderPassCreateInfo::size();
    }
}
